import React, { useState, useCallback, useRef, useEffect } from "react"

import CropImage from '../components/CropImage/dynamic'

const CropPage = () => {
  return <CropImage />
}

export default CropPage
